# 永久定位：单机单用户、本机优先

AssetAgent 永远是单用户、单机、仅 localhost 的个人工具：不做多用户、不做登录、不做公网暴露。多用户会强制引入认证、权限与并发机制，成本远超个人资产场景的收益，且会放大本产品旨在最小化的秘密暴露面。

**后果**：单进程运行、SQLite、无认证、文件挂载密钥都是永久架构选择，不是待还的债。任何需要多租户/远程访问/并发写者的提案，在本 ADR 被取代之前一律视为超出范围。
