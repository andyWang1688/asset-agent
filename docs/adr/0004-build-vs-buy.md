# 优先现成框架，只写标准框架没有的差异化逻辑

新需求先评估有没有成熟的现成框架/组件，能用则用；自己写代码只留给差异化业务逻辑——标准框架提供不了的部分。本项目的自留地是：安全检测/确认闸门/脱敏、知识库编译决策、凭证隔离；通用能力（RAG 检索、对话记忆、压缩、测试、CI 等）一律优先现成（如 ADR 0003 的 LlamaIndex）。

**约束**：引入框架前先钉死边界（框架只进哪个子系统、什么永不许碰），边界写进对应 ADR；越界即放弃引入。
